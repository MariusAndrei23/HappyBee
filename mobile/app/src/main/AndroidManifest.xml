<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.happybee">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HappyBee"
        android:usesCleartextTraffic="true">
        <activity android:name=".FereastraAfisareMain"></activity>
        <activity android:name=".Setari_profil" />
        <activity android:name=".LoginPage" />
        <activity
            android:name=".Pagina_de_info"
            android:label="@string/title_activity_home_page_menu"
            android:theme="@style/Theme.HappyBee.NoActionBar" />
        <activity
            android:name=".HomePageMenuActivity"
            android:theme="@style/Theme.HappyBee.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Despre" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AdaugareStupi" />
        <activity android:name=".FereastraCreareCont" />
    </application>

</manifest>